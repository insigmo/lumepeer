//! Host crate for the integration tests of design doc §17.1.
//!
//! The library itself is intentionally empty: everything lives in `tests/`, in
//! the `/tests/integration` path the repository layout of §6 prescribes.

#![forbid(unsafe_code)]
#![warn(missing_docs)]
