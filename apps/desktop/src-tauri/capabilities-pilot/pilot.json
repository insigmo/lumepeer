{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pilot-window",
  "description": "Debug-only capability for tauri-pilot UI automation. Only ever read when built with `cargo build --features pilot` (see build.rs, which points the capability glob at this directory instead of capabilities/ for that build). Never present in a default or release build.",
  "windows": ["main"],
  "local": true,
  "permissions": ["pilot:default"]
}
